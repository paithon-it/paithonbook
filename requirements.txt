# first list the packages you wish to download from PyPI
teachbooks # depends on jupyter-book
# numpy

# docutils: la versione serve all'impaginazione della pagina di bibliografia.
# Il template TeachBooks pinnava la 0.17.1 (2021), che con il jupyter-book di
# oggi non si risolve piu': `myst-parser` vuole docutils >= 0.18, e pip finisce
# in ResolutionImpossible. Non e' stato un cambio nostro ad accorgersene, e' la
# cache dell'ambiente che nel workflow ruota a settimana: finche' regge, il
# conflitto resta invisibile, e alla prima settimana nuova il build cade.
# Quindi versione esatta e aggiornata, non pin tolta: un intervallo aperto
# rimanderebbe soltanto la prossima rottura a sorpresa.
docutils==0.21.2

# now list the packages (and the respective url) you wish to download from the GitLab package registry
--extra-index-url https://gitlab.tudelft.nl/api/v4/projects/17983/packages/pypi/simple  #temporarily disable
jupyterbook_patches ~= 0.1.4  #temporarily disable
--extra-index-url https://gitlab.tudelft.nl/api/v4/projects/11239/packages/pypi/simple  #temporarily disable
sphinx-thebe ~= 0.9.9  #temporarily disable

# lastly any packages you wish to download directly from a Git repository
download_link_replacer @ git+https://gitlab.tudelft.nl/mude/sphinx-download-link-replacer #temporarily disable

# allows for syncronized tabs in case you’d like your tabs to shift across the page at the same time.
sphinx-inline-tabs 